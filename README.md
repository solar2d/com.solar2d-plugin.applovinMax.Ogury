Sources for the plugin `plugin.applovinMax.Ogury`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.Ogury" = {
            publisherId = "com.solar2d",
        },
    },
}
```
